# welcome.py - A simple Python program that demonstrates how to define and call a function.

# toolbox.py - Contains three Python functions that demonstrate parameters, return values, and default parameters.

# The hardest function to write was the function that uses a default parameter because I had to understand how Python uses a value automatically when no argument is provided. After practicing, I understood how the parameter works and how to call the function correctly.